


function Keepgo() {
    
    const nome = document.querySelector('[name="nome"]').value;
    const email = document.querySelector('[name="email"]').value;
    const assunto = document.querySelector('[name="subject"]').value;
    const descricao= document.querySelector('[name="body"]').value; 
    
    console.log(nome);
}


/*const form = document.getElementById('myForm');

form.addEventListener('submit', (event) => {
  event.preventDefault(); // Impede o envio padrão do formulário

  // Aqui você pode acessar os valores dos campos do formulário
  const nome = document.querySelector('[name="nome"]').value;
  const email = document.querySelector('[name="email"]').value;
  const assunto = document.querySelector('[name="subject"]').value;
  const descricao= document.querySelector('[name="body"]').value; 
    
 

  // Faça o que desejar com os dados (por exemplo, enviar para o servidor)
  console.log('Nome:', nome);
  console.log('E-mail:', email);
});

*/

